import React from "react";
import { LoadingWrapper, LoadingSpinner } from './loading-screen.styles';

export const LoadingScreen = () => {
    return (
        <LoadingWrapper>
            <LoadingSpinner />
        </LoadingWrapper>
    )
}